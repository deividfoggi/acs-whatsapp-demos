{
  "name": "acs-whatsapp-demos",
  "version": "1.0.0",
  "description": "WhatsApp use case demos powered by Azure Communication Services",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspace=backend",
    "lint": "npm run lint --workspace=backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
